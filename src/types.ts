export interface ChzzkAPIBaseUrls {
    chzzkBaseUrl?: string
    gameBaseUrl?: string
}

export interface ChzzkClientOptions {
    nidAuth?: string
    nidSession?: string
    baseUrls?: ChzzkAPIBaseUrls
}